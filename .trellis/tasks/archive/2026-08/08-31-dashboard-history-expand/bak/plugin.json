{
  "name": "token-speed-dashboard",
  "version": "0.1.0",
  "description": "实时显示 ZCode 会话的模型输出速度（tok/s）：零依赖本地仪表盘，Hook 保活 + rollout 日志精确统计",
  "author": { "name": "wangjin" },
  "license": "MIT",
  "keywords": ["token", "speed", "dashboard", "monitor", "tokens-per-second"],
  "userConfig": {
    "openDashboard": {
      "type": "boolean",
      "title": "新会话自动打开仪表盘",
      "description": "收集器启动后的第一个新会话会自动在浏览器打开本地仪表盘（http://127.0.0.1:<port>）。关闭后可随时手动打开，地址见插件数据目录的 port.json。",
      "default": true,
      "required": false
    }
  }
}
